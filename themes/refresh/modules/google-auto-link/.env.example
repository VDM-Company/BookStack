# Google Auto-Link
#
# Log a user straight in when their Google account's verified email matches an
# existing BookStack user, linking the two on first use. On by default whenever
# this module is present; set to false to fall back to stock behaviour, where an
# unknown social account is rejected until the user attaches it from their
# profile settings.
GOOGLE_AUTO_LINK=true
